import React from 'react';
import { Wrapper } from './styles';
import { AddEditFormProps } from './types';
import { Drawer } from 'antd';

export default function AddEditForm(props: AddEditFormProps) {
    const {} = props;
    return <Drawer>AddEditForm</Drawer>;
}
