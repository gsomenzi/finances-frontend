import React from 'react';
import { TransactionsViewProps } from './types';

export default function TransactionsView(props: TransactionsViewProps) {
    return (
        <div>
           <span>TransactionsView</span>
        </div>
    );
}
