import React from 'react';
import { CreditCardsViewProps } from './types';

export default function CreditCardsView(props: CreditCardsViewProps) {
    return (
        <div>
           <span>CreditCardsView</span>
        </div>
    );
}
