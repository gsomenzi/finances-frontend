import React from 'react';
import { TesteViewProps } from './types';

export default function TesteView(props: TesteViewProps) {
    return (
        <div>
            <span>TesteView</span>
        </div>
    );
}
